// Backwards-compatible entry point kept for older local scripts.
// The runtime asset copier now also installs PDF.js, Tesseract, Stockfish,
// and all ONNX Runtime Web WASM files needed by the browser app.
import "./copy-runtime-assets.mjs";
